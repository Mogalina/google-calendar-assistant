version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: google-calendar-assistant-backend:latest
    ports:
      - "8080:8080"                    
    environment:
      - NODE_ENV=production
      - PORT=8080
    restart: always                   
